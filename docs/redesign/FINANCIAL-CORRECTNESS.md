# FINANCIAL-CORRECTNESS — аудит финансовой честности (Итерация 1, §5)

Проверка находок владельца **фактами из кода и данных снапшота 2026-07-23**. Расчётный слой в этой
итерации НЕ меняется — колонка «изменился ли расчёт» = **нет** везде. Реализация подписей и маркеров —
Итерация 3 (§5.1–5.5) и Итерация 6 (§5.6).

## Статус реализации (обновляется по ходу редизайна)

| # | Статус | Что сделано и где (после редизайна) |
|---|---|---|
| 5.1 | ✅ Итерация 3 | Настраиваемый налоговый профиль: селектор **13% / 15% / 0% (ЛДВ·ИИС)** в разделе «Портфель» (`index.html #tax-profile`). Фактор «на руки» — из таблицы ТОЧНЫХ литералов `TAX_OPTIONS` (0.87/0.85/1.0), не `1−rate` → без float-дрейфа; дефолт 13% → `NET_OF_TAX=0.87` → **regression 0**. Влияет на клиентский дивидендный net (акции/портфель/X-Ray, `NET_OF_TAX`+`PFX.TAX`); купонный YTM-net облигаций и форвардная доходность — серверные, помечены «при 13%». Ставка персистится в `dfs.ui.v1.taxRate`. |
| 5.2 | ✅ уже в коде | Горизонт и размер выборки подписаны: X-Ray — «Данные месячные (N мес); дневной риск не оценивается» (`app.js` disc-блок), survivorship-оговорка, винзоризация ±40%. Дополнительные интервалы (β±SE) — по мере переработки X-Ray (Итерация 4). |
| 5.4 | ✅ уже в коде | VaR/риск помечены месячным горизонтом; «дневной риск не оценивается» рядом. Расширение (уровень+горизонт у каждого числа) — Итерация 4. |
| 5.5 | ✅ Итерация 3 (частично) | База gross/net разведена: селектор налога + подписи «на руки»/«до НДФЛ»; в X-Ray reword «Дивдоходность на руки — после НДФЛ {ставка}%; облигационная YTM — при 13%». Единый переключатель Gross/Net/Prob — Итерация 4. |
| 5.3, 5.6, 5.7 | ⏳ | RFR-маркер, облигационные ловушки, «что не учитывается» — Итерации 4/6. |

**Инфраструктура (§6, Итерация 3):** §6.1 кэш-манифест `build.json`+`dataURL()` (25 статичных JSON versioned-кэш, live ISS остаются no-store) — замер [`CACHE-MEASUREMENT.md`](CACHE-MEASUREMENT.md); §6.5 отказоустойчивость ISS — уже была (AbortController+timeout 8/12 c+graceful fallback в `fetchImoexLive`/`fetchStockOHLC`); §6.6 версионируемый `dfs.ui.v1` с миграцией legacy-ключа, портфель `dividendFactorStrategies.myPortfolio.v1` не тронут.

## Сводная таблица

| # | Проблема | Где в коде / данных | Что показываем пользователю | Изменился расчёт |
|---|---|---|---|---|
| 5.1 | НДФЛ захардкожен 13% (`0.87`) | `app.js:835` `NET_OF_TAX=0.87`; `app.js:7312` `PFX.TAX:0.87`; применяется `1474, 1682, 1823, 2693` | Настраиваемый `taxProfile` (13/15/0, ЛДВ, ИИС), badge «после НДФЛ 13%» → настройка; дефолт 13% | нет (дефолт = текущее) |
| 5.2 | Ложная точность на ~90 мес | `returns.json`: 232 тикера × **91 мес** (min=med=max); `app.js:1550-1551,2049-2050` Sharpe/Sortino 2 знака; `1976,2047,2129` beta 2 знака без интервала | `n=91 мес` + надёжность у метрики; β с интервалом ±SE; Sharpe/Sortino 1 знак или 2 с интервалом | нет (новая функция SE рядом с `drBeta`) |
| 5.3 | RFR — точка, не ряд | `app.js:1288,1774` `MARLAMOV.meta.rfr`; `1775` fallback `DATA.meta.rf_ofz`; предупреждение `1922` в общем списке | Маркер `RFR-const` у Sharpe/Sortino/alpha/спреда с tooltip (значение, дата, почему нет ряда, смещение) | нет |
| 5.4 | Горизонт VaR | месячный движок; `app.js:2053` «VaR 95% (мес)», `2072/2076` раздельные details дневной/месячный | Уровень+горизонт у КАЖДОГО числа как часть `.metric-card`; покрытие «дневной риск: N из M»; chip метода | нет |
| 5.5 | Дивпоток — 3 разных числа | `dividendPortfolioGross` (gross); `NET_OF_TAX` (net); `app.js:2666` `payout_prob=1−cut_risk` (prob) | Единый переключатель Gross / После НДФЛ / С учётом вероятности + подпись отсечка≠зачисление, T+1 | нет |
| 5.6 | Облигации: ловушки рынка РФ | `bonds/screener.json` (20 бумаг) | маркеры + «Ограничения» (см. ниже) | нет |
| 5.7 | Чего нет в модели портфеля | — | Блок «Что не учитывается» (свёрнут) | нет |

## §5.6 — детально (подтверждение/опровержение по полям)

`bonds/screener.json` содержит: `secid, isin, name, coupon_pct, freq, maturity, duration_years, face,
lot_value, price_market, price_fair, ytm_market, ytm_fair, **ytm_net**, valtoday, rating*`.

| Требование §5.6 | Поле в данных | Вердикт | Действие |
|---|---|---|---|
| Оферта (put/call, YTP, дата) | **нет** | ⚠️ подтверждено: поля отсутствуют | маркер `оферта не проверена` на карточке; в «Ограничения» |
| Флоатер (тип купона, база, спред, пересчёт) | **нет** (`coupon_pct` — фикс-значение) | ⚠️ подтверждено | не сортировать в колонке YTM с фиксами без разделения; маркер «купон не классифицирован» |
| Амортизация | **нет** | ⚠️ подтверждено | маркер; дюрация/YTM амортизируемых — с оговоркой |
| НКД / чистая vs грязная цена | **нет** (`price_market` — без пометки) | ⚠️ подтверждено | подписать, какая цена показана |
| Ликвидность (оборот, bid/ask) | `valtoday` есть; **bid/ask нет** | частично | показать `valtoday` рядом с YTM; спред bid/ask — в «Ограничения» |
| G-спред (база КБД, дата) | **нет** (`deviation` — иное) | ⚠️ подтверждено | не показывать как G-спред; если нужен — в «Ограничения» |
| Net-of-tax YTM | **`ytm_net` есть** (из Python-пайплайна) | ✅ | НЕ пересчитывать (§5.1.5) — только подписать «после НДФЛ 13%» |

**Вывод §5.6:** критичные для розницы поля (оферта/флоатер/амортизация/НКД/bid-ask/G-спред) в данных
ОТСУТСТВУЮТ. По правилу спеки (§2, §5.6) — не выдумывать: явные маркеры «не проверено» + раздел
«Ограничения». Ввод этих полей = задача Python-пайплайна облигаций (вне скоупа фронта).

## §5.7 — чего нет в модели портфеля (подтверждено чтением `pfxCompute`)

Не моделируются: брокерские/биржевые комиссии; валютная экспозиция (замещающие/юаневые); денежная
позиция; таймер ЛДВ по позиции; налог на купон; реинвестирование дивидендов в total return. →
блок «Что не учитывается» (свёрнут) на экране портфеля.

## Что уже сделано ЧЕСТНО (не ломать при подаче)

- Бенчмарк — MCFTR (total return), ресемпл в месячный; ценовой P&L отделён от total-return метрик.
- `DR_EXCL` + confidence-бэйджи (`pfxConfBadge`): бумаги без чистой истории исключаются из VaR (SNGS в
  фикстуре → `no_data`), покрытие `pf.covered` подписано.
- Дневной VaR — честно `unavailable` при отсутствии дневного слоя; месячный и дневной разведены в
  разные details с подписью горизонта.
- `payout_prob = 1−cut_risk` подписан как ML-оценка, сценарии — «диагностические, не прогноз».
