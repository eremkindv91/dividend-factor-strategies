# FINAL-REPORT — итог редизайна (Итерация 7)

Ветки `redesign/*` (по одной на итерацию) от `dividend-site`. Расчётный слой не менялся ни в одной
итерации: `node tools/regression.js --mode=check` → **0 расхождений** по всем baseline.

## Definition of Done (§25)

| Критерий | Статус | Заметка |
|---|---|---|
| regression --mode=check = 0 | ✅ | 0 расхождений (проверено на каждой итерации + финально) |
| console-report.json: 0 errors/pageerror/failed/rejections | ✅ | `artifacts/console-report.json` — всё 0 по 9 разделам (с CSP) |
| Нет h-scroll на 360/390/768/1024/1280/1440/1920 | ✅ | 0 из 63 сочетаний |
| Lighthouse a11y ≥90, Best Practices ≥90 | ⚠️ | Lighthouse локально не установлен — не прогонялся в этой среде. Внесены конкретные a11y-меры (ниже); прогон — в Chrome DevTools на живом сайте |
| 9 разделов открываются; dialog с клавиатуры | ✅ | showModal() → Escape + focus-trap + возврат фокуса (проверено) |
| hash-ссылки + дивидендные deep links | ✅ | все разделы + overview-алиас; deep links не тронуты |
| Импорт CSV; портфель из СТАРОГО ключа localStorage | ✅ | CSV распарсил 2 позиции; `dividendFactorStrategies.myPortfolio.v1` читается |
| Работа под подпутём (GitHub Pages, §4.2) | ✅ | все пути относительные (`data.json`, `bonds/…`, `dataURL`); CSP `default-src 'self'` подпуть-нейтрален |
| Повторный визит из кэша (замер до/после) | ✅ | 23/28 data-JSON из кэша — [CACHE-MEASUREMENT.md](CACHE-MEASUREMENT.md) |
| Каждая метрика: горизонт/база/надёжность/дата | 🟡 | ключевые подписаны (налоговая база gross/net, горизонт «мес», даты, надёжность выборки в X-Ray); часть глубоких метрик остаётся краткой |
| VaR без уровня и горизонта — нет | ✅ | «VaR 95% (мес)» / «CVaR 95% (мес)» везде с уровнем+горизонтом |
| Облигации оферта/амортизация/флоатер промаркированы | ✅ | §5.6: маркеры «не проверено» + блок «Ограничения данных» |
| Налоговая ставка настраиваема, дефолт 13%, regression не изменилась | ✅ | 13/15/0%, дефолт → NET_OF_TAX=0.87 → regression 0 |
| Источники данных и даты сохранены и показаны | ✅ | freshness-чипы + `#data-status` в topbar сохранены |
| Полный проход сценариев с клавиатуры | 🟡 | focus-visible + диалоги + вкладки навигируемы клавиатурой; исчерпывающий проход каждого потока не автотестировался |
| CSS+JS не выросли >20% | ✅ | JS+CSS **+5.4%** (app +3.8%, styles +10.4%, index +17.1%) |
| Скриншоты before/after во всех вьюпортах | ✅ | before (Итерация 0) + after-iter7 — по 63 |
| Не воспроизводит идентичность Т-Инвестиций | ✅ | собственная система (бренд «Аналитика рынка РФ») |
| COMPONENTS.md описывает дизайн-систему | ✅ | [COMPONENTS.md](COMPONENTS.md) |

Легенда: ✅ выполнено · 🟡 частично (честно) · ⚠️ не проверено в этой среде.

## Безопасность (§6.4)

- **CSP** (`<meta http-equiv>`): `default-src 'self'`; `script-src` + jsdelivr/unpkg (Chart.js/LWC on-demand);
  `connect-src 'self' https://iss.moex.com`; `img-src 'self' data:`; `style-src 'self' 'unsafe-inline'`
  (44 динамических inline-стиля); `object-src 'none'`; `form-action 'none'`; `base-uri 'self'`.
  Проверено: графики, CSV (blob), ISS-fetch проходят; **0 CSP-violation** в консоли по всем разделам.
- **rel="noopener noreferrer"** на всех `target="_blank"` — 11/11 в app.js + 1 в index.html.
- **XSS-guard** (`tools/xss-guard.js` → `artifacts/xss-guard.json`): эвристика по `${}` в шаблонах.
  2041 интерполяция, 822 форматированных/безопасных, 1219 «на ревью» (консервативно: класс-имена,
  константы, join флагов — не уязвимости; данные из доверенных JSON, юзер-ввод портфеля через esc).

## Доступность — внесённые меры (§21)

Видимый `:focus-visible` глобально (клавиатурный, не мышиный) · `aria-current` на разделах ·
`aria-selected` на вкладках стратегий и X-Ray · `<dialog>` через showModal (Escape+focus-trap) +
возврат фокуса на триггер + закрытие по бэкдропу · touch targets ≥48px в нижней навигации ·
расширенный `prefers-reduced-motion` (гасит все анимации/переходы) · `.pro-card` — role=button +
tabindex + keydown(Enter/Space, preventDefault).

## Производительность (§23)

- JS+CSS: 761 КБ → 802 КБ (+5.4%, бюджет +20%). Никаких новых тяжёлых библиотек и icon-фонтов.
- Повторный визит: 23/28 data-JSON из кэша (versioned `?v=` вместо `?t=Date.now()`) — главный выигрыш.
- Debounce поиска акций 250 мс; ленивый init графиков (Chart.js/LWC on-demand, только видимая вкладка).

## §26 — наблюдения по сценариям

**A. Новый пользователь (1440×1000, above the fold).** Sidebar с разделами (Обзор активен) · topbar
«Обзор» + свежесть данных + «Не ИИР» · Market Pulse (фаза рынка словами) · gauge напряжения ·
KPI (MCFTR, от экстремума) · карточка P/E. → видно: что за продукт, что с рынком, где разделы
(Портфель/Акции в sidebar). На 390×844: topbar + Market Pulse + KPI стопкой + нижняя навигация
(Обзор/Портфель/Акции/Стратегии/Ещё).

**B. Владелец портфеля (заполненный X-Ray, above the fold).** Итог инвесткомитета (health-score +
вердикт) · сильные стороны / главные риски / что проверить · headline KPI-лента · «Что требует
внимания» · «Сегодня важно для портфеля». → читается: состояние, риски, что требует внимания.

**C. Продвинутый инвестор (число кликов, цель ≤3).** Подробный риск-анализ: Портфель → вкладка Риск
(2). SHAP: Акции → строка бумаги (2, инлайн-детали). Факторная стратегия: Стратегии → карточка (2).
График инструмента: Акции → строка → блок графика (2). Методология показателя: tooltip у метрики /
Методология (1–2).

**D. Финансовая честность.** Каждое число X-Ray сопровождается контекстом: горизонт («мес»), база
(gross/net через налоговый селектор), частота (месячная), размер выборки (N мес), метод (в модуле),
дата (снапшот). Блок «Данные и ограничения» — исключённые позиции и покрытие.

## Осознанно отложено (честно, вне узких exit-критериев)

- §14: первый уровень «карточки стратегий» + пошаговый конструктор-визард (сейчас — рабочий
  конструктор с переключателем режимов + честные подписи данных/лага).
- §15: карта банков P/BV×ROE и карточки сравнения (раздел работает, грузится лениво).
- Идеальный CSP `'self'`-only (требует вендоринга Chart.js/LWC и рефактора inline-стилей).
- Прогон Lighthouse (нет CLI в среде) — выполнить в Chrome DevTools на живом сайте.
